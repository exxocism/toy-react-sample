{
  "compilerOptions": {
    "outDir": "./build/",
    "noImplicitAny": true,
    //commonjs = require, es6 = import statement
    "module": "es6",
    //Note that es5 doesn't support class declations
    "target": "es6",
    "jsx": "react",
    "allowJs": true,
    "moduleResolution": "node",
    "allowSyntheticDefaultImports": true,
    "esModuleInterop": true,
    "types": ["react/next", "react-dom/next", "@types/node"]
    //only if you want type safety with the backend
    // "paths": { "@backend/*": ["../server/*"] }
  }
}
